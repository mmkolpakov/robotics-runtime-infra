# Synthetic source for the private CI network, never a production NTP server.
local stratum 10
port 1123
allow
driftfile /tmp/chrony.drift
pidfile /tmp/chronyd.pid
cmdport 0
bindcmdaddress /tmp/chrony/chronyd.sock
user _chrony
