# A real client of the isolated CI source; no host clock adjustment (-x).
server 127.0.0.1 port 1123 iburst minpoll -4 maxpoll -4
driftfile /tmp/chrony.drift
pidfile /tmp/chronyd.pid
cmdport 0
bindcmdaddress /run/robotics-time/chronyd.sock
user _chrony
