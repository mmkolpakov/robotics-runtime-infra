[pytest]
; `record_property` (used by infra/smoke/launch_testing/test_joint_motion.py
; to record metrics as JUnit XML <property> tags) is only supported by the
; "legacy"/xunit1 JUnit XML family, not the newer xunit2 default.
junit_family = legacy
